#pragma once

#include "types.h"

#define ASSET(x, y) static const AssetID x = { y };
struct Asset
{
	struct Model
	{
		static const size_t count = @model_count@;
		static const char* filenames[count];
@models@	};
	struct Texture
	{
		static const size_t count = @texture_count@;
		static const char* filenames[count];
@textures@	};
	struct Shader
	{
		static const size_t count = @shader_count@;
		static const char* filenames[count];
@shaders@	};
};
#undef ASSET
